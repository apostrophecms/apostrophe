---
"apostrophe": minor
---

The moment the site starts listening is now the `apos-listening` event carrying the local and admin URLs, drawn as a startup banner in development, emitted as one JSON object in structured mode and kept by the default filter in production.
