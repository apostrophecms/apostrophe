const assert = require('assert');

describe('eslint-config-apostrophe:canary', function () {
  it('should pass a canary test', function () {
    // eslint-disable-next-line
    assert.equal(true, true);
  });
});
